#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
*main - print the vocabulario
*Return: return zero
*/

int main(void)
{
char zyx = 'z';
while(zyx => 'a')
{
putchar(zyx);
zyx++;
}
putchar(10);
return (0);
}
