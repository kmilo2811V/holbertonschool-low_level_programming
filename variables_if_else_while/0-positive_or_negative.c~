#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - the function find the positive, negativo o zero of n
 *Return: 0
 */
int main(void)
{
  int

    strand((time))(0);
  n =rand() -Rand_max / 2
    if (f ==0)
