#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - printf vocavulary excep q and e
 *
 *Return: return zero
 */

int main(void)
{
int abc = 'a';
while (abc <= 'd')
while (abc <= 'p')
{
putchar(abc);
abc++;
}
putchar('\n');
return (0);
}
