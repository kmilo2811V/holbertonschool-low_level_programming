#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/* This program will assign a random number to the variable n each time it is execut\
   ed.*/
*
int main(void)
{
  int n;

  srand(time(0))
    n = rand() - RAND_MAX / 2;
  RETURN (0);
}
